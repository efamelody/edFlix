CREATE TABLE courses (
    id INTEGER PRIMARY KEY,
    name TEXT,
    description TEXT,
    yeargroup TEXT,
    degree TEXT,
    paid TEXT,
    certificate TEXT,
    interest TEXT
);

-- Insert records of data into the database:

